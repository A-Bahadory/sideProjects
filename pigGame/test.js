const score = [0, 0];
console.log(score.indexOf(0), "index");
console.log(score.indexOf(0), "index");
let activePlayer = 0;
console.log(activePlayer, "active player");

score[activePlayer] += 12;
console.log(score);

const testArr = [1, 2, 3];
console.log(testArr);
testArr[1] += "this is a new assignment";
console.log(testArr);
